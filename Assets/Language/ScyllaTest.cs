using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScyllaTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Scylla.run("number a = }");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
